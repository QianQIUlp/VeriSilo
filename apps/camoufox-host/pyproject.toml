[project]
name = "verisilo-camoufox-host"
version = "0.1.0"
description = "M0 Camoufox compatibility spike host. Spike-only; not a production EngineAdapter."
requires-python = ">=3.12,<3.13"
dependencies = [
    "camoufox==0.5.4",
    "playwright==1.60.0",
    "browserforge==1.2.4",
    "tzdata>=2025.2",
]

[tool.uv]
package = false
