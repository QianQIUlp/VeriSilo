/**
 * Keep the public project destination in one place so the desktop and browser
 * surfaces cannot silently drift. Switch this to https://verisilo.qiu.works/
 * only after its DNS, TLS certificate, and landing page are live.
 */
export const PRODUCT_WEBSITE_URL =
  "https://github.com/QianQIUlp/VeriSilo" as const;
